// Child
function greet(name) {
    return "Hello " + name;
}

// Parent
const greetAndrew = greet("Andrew"); //Scope 1
const greetAbraham = greet("Abraham"); //Scope 2

<ConfirmButton />
<ConfirmButton      message={"are you sure"} />
<ConfirmButton      message={"are you delete"} />
<ConfirmButton      message={"are you lout"} />
